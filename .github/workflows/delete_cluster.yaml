on:
 push:
   branches:
   - main
   paths:
   - 'test/delete_cluster1.yaml'

jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: test
          run: |
          
            mkdir -p folder
            var=$( sed -n 1p test/DELETE )
            cd folder
            git clone https://github.com/maxxam123/platform01.git
            rm -rf .github/workflows/$var.yaml

            cp ../templates/pipelines/delete_pipeline .github/workflows/$var.yaml
            sed -i -e "s/NAME/$var/g" .github/workflows/$var.yaml

#        - name: Pushes to another repository
#          uses: cpina/github-action-push-to-another-repository@main
#          env:
#            API_TOKEN_GITHUB: ${{ secrets.ACCESS_TOKEN }}
#          with:
#            source-directory: "folder"
#            destination-github-username: "maxxam123"
#            destination-repository-name: "platform01"
#            user-email: hennighausenmax@gmail.com
#            target-branch: main#
